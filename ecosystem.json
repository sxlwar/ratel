{
    "apps": [
        {
            "name": "ratel",
            "script": "",
            "env": {
                "COMMON_VARIABLE": "true"
            },
            "env_production": {
                "NODE_ENV": "production"
            }
        }
    ],
    "deploy": {
        "production": {
            "user": "yang",
            "host": ["hijavascript.com"],
            "port": "22",
            "ref": "origin/master",
            "repo": "git@gitee.com:sxlwar/ratel.git",
            "path": "/www/web/production",
            "ssh_options": "StrictHostKeyChecking=no",
            "post-deploy": "npm install --production && pm2 reload ecosystem.json --env production && npm run build --aot=false --build-optimizer=false --max_old_space_size=8192",
            "env": {
                "NODE_ENV": "production"
            }
        }
    }
}
